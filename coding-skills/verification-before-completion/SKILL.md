---
name: verification-before-completion
description: 在准备声称工作完成、修复或通过时使用，在提交或创建 PR 之前 - 要求在做出任何成功声明之前运行验证命令并确认输出；总是先有证据再有断言
---

# 完成前验证

## 概述

没有验证就声称工作完成是不诚实，不是效率。

**核心原则：** 证据在声明之前，始终如此。

**违反此规则的字面意思就是违反此规则的精神。**

## 铁律

```
没有新鲜验证证据就不能声称完成
```

如果你在这条消息中没有运行验证命令，你不能声称它通过了。

## 门禁函数

```
在声称任何状态或表达满意之前：

1. 识别：什么命令能证明这个声明？
2. 运行：执行完整命令（新鲜的、完整的）
3. 阅读：完整输出，检查退出代码，计算失败数
4. 验证：输出是否确认声明？
   - 如果否：陈述实际状态并附证据
   - 如果是：陈述声明并附证据
5. 然后才：做出声明
```

跳过任何步骤 = 撒谎，不是验证

## 常见失败

| 声明 | 需要 | 不充分 |
|------|------|--------|
| 测试通过 | 测试命令输出：0 个失败 | 上次运行、"应该通过" |
| 代码检查干净 | 代码检查输出：0 个错误 | 部分检查、推断 |
| 构建成功 | 构建命令：退出 0 | 代码检查通过、日志看起来正常 |
| Bug 已修复 | 测试原始症状：通过 | 代码已更改、假设已修复 |
| 回归测试有效 | 红绿循环已验证 | 测试通过一次 |
| 代理已完成 | VCS diff 显示更改 | 代理报告"成功" |
| 满足需求 | 逐条检查清单 | 测试通过 |

## 红线 - 停下

- 使用"应该"、"可能"、"似乎"
- 在验证之前表达满意（"太好了！"、"完美！"、"完成了！"等）
- 即将提交/推送/创建 PR 但没有验证
- 信任代理成功报告
- 依赖部分验证
- 想着"就这一次"
- 疲惫了想让工作结束
- **任何暗示成功但未运行验证的措辞**

## 合理化预防

| 借口 | 现实 |
|------|------|
| "现在应该能用了" | 运行验证 |
| "我很自信" | 自信 ≠ 证据 |
| "就这一次" | 没有例外 |
| "代码检查通过了" | 代码检查 ≠ 编译器 |
| "代理说成功了" | 独立验证 |
| "我累了" | 疲劳 ≠ 借口 |
| "部分检查够了" | 部分证明不了什么 |
| "措辞不同所以规则不适用" | 精神优于字面 |

## 关键模式

**测试：**
```
✅ [运行测试命令] [看到：34/34 通过] "所有测试通过"
❌ "现在应该通过了" / "看起来正确"
```

**回归测试（TDD 红绿）：**
```
✅ 编写 → 运行（通过）→ 回退修复 → 运行（必须失败）→ 恢复 → 运行（通过）
❌ "我写了一个回归测试"（没有红绿验证）
```

**构建：**
```
✅ [运行构建] [看到：退出 0] "构建通过"
❌ "代码检查通过了"（代码检查不检查编译）
```

**需求：**
```
✅ 重新阅读计划 → 创建检查清单 → 验证每条 → 报告差距或完成
❌ "测试通过，阶段完成"
```

**代理委派：**
```
✅ 代理报告成功 → 检查 VCS diff → 验证更改 → 报告实际状态
❌ 信任代理报告
```

## 为什么这很重要

来自 24 次失败记忆：
- 人类搭档说"我不相信你" - 信任破裂
- 未定义函数被提交 - 会崩溃
- 缺失需求被提交 - 功能不完整
- 时间浪费在虚假完成 → 重定向 → 返工上
- 违反："诚实是核心价值观。如果你撒谎，你会被替换。"

## 何时应用

**总是在以下之前：**
- 任何成功/完成声明的变体
- 任何满意表达
- 任何关于工作状态的正面陈述
- 提交、创建 PR、完成任务
- 进入下一个任务
- 委派给代理

**规则适用于：**
- 确切措辞
- 释义和同义词
- 暗示成功
- 任何暗示完成/正确性的沟通

## 底线

**验证没有捷径。**

运行命令。阅读输出。然后声称结果。

这是不可协商的。
